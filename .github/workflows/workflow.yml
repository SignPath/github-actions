name: Build

on:
 push:
 pull_request:

jobs:
  build:
    runs-on: windows-latest

    steps:
    
    - name: checkout
      uses: actions/checkout@v4

    - name: install dev dependencies
      run: npm install

    - name: build the actions
      run: npm run build